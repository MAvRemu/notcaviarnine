# not-caviarnine — working notes

Next.js (App Router, TypeScript) app, deployed on Vercel.

Status: freshly scaffolded 2026-08-26 with create-next-app. Purpose and
details TBD — to be filled in as requirements arrive.

## How to work on it

```bash
npm run dev
npm run build
npm run lint
```

Secrets live in `.env.local` (gitignored); `.env.example` is the template.
